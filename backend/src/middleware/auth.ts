/** @deprecated Use `../core/auth` */
export type { AuthenticatedRequest as AuthRequest } from "../core/auth/types";
export { requireAuth, requireAdmin, optionalAuth, signToken } from "../core/auth";
