{
  "name": "gildia-vercel-entry",
  "version": "0.1.0",
  "private": true,
  "description": "Vercel Root Directory bridge — real app is in repository root (../)",
  "scripts": {
    "build": "cd .. && npx prisma generate && npm run build",
    "dev": "cd .. && npm run dev",
    "start": "cd .. && npm start"
  },
  "dependencies": {
    "next": "14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
